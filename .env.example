# LM Studio Configuration (Local Model - No OpenAI API Key Needed)
# On AWS: LM Studio runs on the host, containers connect to localhost:1234
# On Local: Make sure LM Studio is running and accessible on port 1234
LOCAL_MODEL_API=http://localhost:1234/v1
OPENAI_API_KEY=local-model
OPENAI_MODEL=model-identifier

# Marketplace Configuration
DEFAULT_CURRENCY=PLN
POST_MODE=dry_run
HEADLESS=true

# Optional marketplaces (set to true to enable)
ENABLE_OLX=true
ENABLE_FACEBOOK=true

# Persistent paths (Docker: /app/data, Local: ./browser_profiles)
USER_DATA_DIR=/app/data/browser_profiles

# Docker/API configuration
API_HOST=0.0.0.0
API_PORT=8000
STREAMLIT_PORT=8501
